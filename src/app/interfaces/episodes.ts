export interface Episodes {/*interface de los episodios*/
    id: number;
    name: string;
    airDate: string;
    episodeCode: string;
    characters: string[];
    url: string;
    created: string;
}
